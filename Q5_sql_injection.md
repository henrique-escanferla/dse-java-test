# Question 5 – SQL Injection

SQL Injection is prevented by using prepared statements, ORM frameworks,
and avoiding dynamic SQL string concatenation. Database access should
follow the principle of least privilege.
